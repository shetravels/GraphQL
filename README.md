# bump to symfony 6

Utilisation de rector pour mettre à jour l'ensemble du code de manière automatique jusqu'à la version Symfony de 6.

Les tests ne fonctionnent plus cowboy

```bash
symfony php vendor/rector/rector/bin/rector process src
```
